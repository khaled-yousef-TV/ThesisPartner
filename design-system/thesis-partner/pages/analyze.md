# Page override: Analyze

Overrides global pattern for the **draft analysis** workspace (not a marketing landing).

- **Layout:** Two-column from `1024px`: left binder nav (~260px), right stack (form + results). Single column on narrow viewports; binder becomes collapsible `<details>` or top drawer pattern.
- **Hero:** None. Page title + short helper text only.
- **Primary surface:** Large monospace or `Atkinson Hyperlegible` textarea; min height `280px`; `border-radius: 8px` per Master inputs.
- **Results:** Accordion / `<details>` groups for Grammar, APA 7, Theme fit, AI scan — flat cards, no heavy shadow (Flat Design).
