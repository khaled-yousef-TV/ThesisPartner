# Page override: Chat (thesis memory)

- **Layout:** Message list (scrollable) + single-line composer; assistant bubbles use `--color-background`; user bubbles use white + `--color-primary` border.
- **Density:** Compact vertical rhythm (`--space-sm` between turns) for long conversations.
